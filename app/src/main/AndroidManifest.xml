<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fadhlan.uts_10120012">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_fadhlan"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_fadhlan_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UTS10120012DailyNotes">
        <activity android:name=".main.profile.ProfileActivity"></activity>
        <activity android:name=".main.diary.UpdateActivity" />
        <activity android:name=".main.diary.SaveActivity" />
        <activity android:name=".main.diary.NotesActivity" />
        <activity android:name=".main.diary.ListNoteActivity" />
        <activity android:name=".main.diary.SaveCategoryActivity" />
        <activity android:name=".main.diary.CategoriActivity" />
        <activity
            android:name=".main.viewpager.ViewPagerActivity"
            android:theme="@style/SplashScreenStyle"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>